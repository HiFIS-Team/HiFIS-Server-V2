"""서버 테스트 — **DB 도 서버도 안 띄운다.**

여기 있는 것은 전부 순수 계산이다 (`app/api/staff/attendance.py` 의 판정,
`app/services/payroll.py` 의 급여 주기 …). `pytest` 만 있으면 어디서나 돈다.

## 왜 이것부터인가

돈 계산과 근태 판정이 이 레포에 있는데 자동 검증이 하나도 없었다.
대신 [.claude/backend-gap.md](../.claude/backend-gap.md) 에 **사람이 한 번
손으로 재 본 표**가 잔뜩 적혀 있다 —

    17:00 조기퇴근 · 17:39 조기퇴근 · 17:40 퇴근 · 18:00 퇴근
    평일 09~18   21일 × 540분 = 189.0h → 1,950,480원   (검산 일치)

**그게 곧 테스트다.** 실행되지 않는 자리에 있어서, 코드를 고치면 아무도
다시 안 재 볼 뿐이다. 그 표를 여기로 옮긴다.

## 늘릴 때

DB 가 필요한 것은 여기 넣지 않는다 — 붙이는 순간 "돌리려면 준비가 필요한"
테스트가 되고, 그러면 안 돌린다. 순수 함수가 196개 있으니 한동안 그걸로 충분하다.
"""
